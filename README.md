## WIP, be careful and stuff

---

| | Sudosave | |
---
| | save things as root using `sudo`, `doas`, or `su` | |

This (will, it's not even started yet) opens an interactive terminal for saving files as root.
